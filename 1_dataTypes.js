"use strict";

// Counting elements of different types in an array.

/*
const arr = [true, 'hello', 5, 12, -200, false, false, 'word'];
let set = {"number" : 0, "string" : 0, "boolean" : 0};

	for (let value of arr){

		if (typeof value === "number"){
			set.number++;
		} else if(typeof value === "string"){
			set.string++;
		} else {
			set.boolean++;
		}
	}
console.log(set);
*/

/*
function inc(num) {
	const Num = {n: 5};
	const obj = Object.create(Num);
	obj["n"] = 6;
		console.dir(obj);
	}
		
inc();
*/

// function inc(n){
// 		const a = 5;
// 		const b = a+1;	
// 		console.dir({a, b});
// 		}	
// inc();

/*
const fn = null;
const fn = () => {
  console.log({ a });
  var a = 5;
};

console.log(fn);
*/